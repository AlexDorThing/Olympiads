<UserControl x:Class="SchoolOlymps.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <TabControl>
        <!-- ========== Вкладка Пользователи ========== -->
        <TabItem Header="Пользователи">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="UserSearch" Grid.Row="0" Margin="5" TextChanged="UserSearch_TextChanged"/>
                <DataGrid x:Name="UsersGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                        <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="100"/>
                        <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="150"/>
                        <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="100"/>
                        <DataGridTextColumn Header="Школа" Binding="{Binding School.Name}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                    <Button Content="Редактировать" Click="EditUser_Click"/>
                    <Button Content="Удалить" Click="DeleteUser_Click"/>
                </StackPanel>
            </Grid>
        </TabItem>

        <!-- ========== Вкладка Школы ========== -->
        <TabItem Header="Школы">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="SchoolSearch" Grid.Row="0" Margin="5" TextChanged="SchoolSearch_TextChanged"/>
                <DataGrid x:Name="SchoolsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="200"/>
                        <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                    <Button Content="Редактировать" Click="EditSchool_Click"/>
                    <Button Content="Удалить" Click="DeleteSchool_Click"/>
                </StackPanel>
            </Grid>
        </TabItem>

        <!-- ========== Вкладка Олимпиады ========== -->
        <TabItem Header="Олимпиады">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="OlympiadSearch" Grid.Row="0" Margin="5" TextChanged="OlympiadSearch_TextChanged"/>
                <DataGrid x:Name="OlympiadsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                        <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                        <DataGridTextColumn Header="Предмет" Binding="{Binding Subject.Name}" Width="100"/>
                        <DataGridTextColumn Header="Макс. балл" Binding="{Binding MaxPoints}" Width="80"/>
                        <DataGridTextColumn Header="Организатор" Binding="{Binding Organizer.FullName}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                    <Button Content="Редактировать" Click="EditOlympiad_Click"/>
                    <Button Content="Удалить" Click="DeleteOlympiad_Click"/>
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>